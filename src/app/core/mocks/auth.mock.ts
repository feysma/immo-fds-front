/**
 * Identifiants mock pour le compte administrateur de démonstration.
 * Utilisés à la fois par AuthService (validation) et LoginComponent (auto-remplissage).
 */
export const MOCK_CREDENTIALS = {
  email: 'admin@immofds.be',
  password: 'Admin@2026!',
} as const;
