/**
 * Flag global de mock pour toute l'application.
 * Passer à false pour consommer l'API réelle.
 */
export const USE_MOCK = true;

/**
 * Active l'autocomplete d'adresse via Nominatim/OpenStreetMap.
 * Passer à false pour désactiver (aucun appel réseau externe).
 */
export const USE_ADDRESS_AUTOCOMPLETE = true;
